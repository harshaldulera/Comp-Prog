#include <bits/stdc++.h>
using namespace std;

#define PI 3.14
#define long long ll

class Stack {
    // properties
    public:
        int *arr;
        int top;
        int size;

    // behvaiour
    Stack(int size) {
        this -> size = size;
        arr = new int[size];
        top = -1;
    }

    void push (int element) {
        // if(top2 - )
    }

    void pop () {

    }

    int peek() {

    }

    bool isEmpty() {

    }
};

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}